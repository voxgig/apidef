{
  "name": "@voxgig/apidef",
  "version": "6.2.0",
  "main": "ts/dist/apidef.js",
  "type": "commonjs",
  "types": "ts/dist/apidef.d.ts",
  "description": "Voxgig SDK Generator.",
  "homepage": "https://github.com/voxgig/voxgig-apidef",
  "keywords": [
    "voxgig",
    "apidef",
    "api",
    "sdk"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/voxgig/apidef.git"
  },
  "bin": {
    "voxgig-apidef": "bin/voxgig-apidef"
  },
  "scripts": {
    "test": "node --enable-source-maps --test \"ts/dist-test/**/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$TEST_PATTERN\" --test \"ts/dist-test/**/*.test.js\"",
    "watch": "tsc --build ts/src ts/test -w",
    "build": "tsc --build ts/src ts/test",
    "clean": "rm -rf ts/dist ts/dist-test node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-tag-dry": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo \"[dry-run] TAG: v$REPO_VERSION\" && if git rev-parse -q --verify refs/tags/v$REPO_VERSION >/dev/null; then echo \"[dry-run] WARNING: tag v$REPO_VERSION already exists - bump the version first\"; else echo \"[dry-run] would commit -a, push, tag v$REPO_VERSION and push --tags; nothing done\"; fi && git status --short",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-dry": "npm run clean && npm i && npm run repo-publish-quick-dry",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --registry https://registry.npmjs.org --access=public",
    "repo-publish-quick-dry": "npm run build && npm run test && npm run repo-tag-dry && npm publish --dry-run --registry https://registry.npmjs.org --access=public"
  },
  "license": "MIT",
  "files": [
    "model",
    "ts/src",
    "ts/dist",
    "bin",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "25.9.2",
    "typescript": "6.0.3"
  },
  "peerDependencies": {
    "@jsonic/yaml": ">=0",
    "@voxgig/struct": ">=0",
    "@voxgig/util": ">=0",
    "aontu": ">=0",
    "jostraca": ">=0",
    "jsonic": ">=2",
    "shape": ">=10"
  },
  "dependencies": {
    "diff": "9.0.0",
    "ordu": "4.3.0",
    "pino": "10.3.1",
    "pino-pretty": "13.1.3"
  }
}
