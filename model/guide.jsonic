
guide: entity: &: {
  active?: boolean
  name: key()

  path: &: {
    active?: boolean
    
     op: &: {
       active?: boolean

       # TODO: aontu error msg
       # Does not identify missing peer node path
       method: string

       transform?: {
         req?: top
         res?: top
       }
     }
  }
}




