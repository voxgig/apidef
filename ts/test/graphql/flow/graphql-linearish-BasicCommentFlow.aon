# BasicCommentFlow

main: kit: flow: BasicCommentFlow:
{
  entity: "comment",
  kind: "basic",
  name: "BasicCommentFlow",
  step: [
    {
      input: {
        ref: "comment_ref01"
      }

      op: "create"
    }

    {
      input: {
        ref: "comment_ref01",
        suffix: "_rm0"
      }

      match: {
        id: "comment01"
      }

      op: "remove"
    }
  ],
  "key$": "BasicCommentFlow"
}