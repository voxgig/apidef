
* in points, use select:query:{}, query is just the Struct schema
** thus point selection becomes a simple schema match
* fix field merge - only use actual entity fields - remove action fields
* same peer deps as sdkgen
